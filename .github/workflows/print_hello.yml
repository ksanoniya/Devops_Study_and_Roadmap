name: gitaction hello world

on: 
    push:
        branches:
         - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: print Hello world
        run: echo "hello world"
